name: Client
on:
    push:
        paths:
            - 'packages/client/**'
            - '.github/workflows/client.yml'
jobs:
    client:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3
            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: 16.14.2
                  cache: 'yarn'
            - name: Install dependencies
              run: yarn
            - name: Build Bundle
              run: yarn workspaces foreach client build
            - name: Run Test
              run: yarn workspaces foreach client test
