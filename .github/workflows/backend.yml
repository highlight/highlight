name: Backend
on:
    push:
        paths:
            - 'backend/**'
jobs:
    backend:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2
            - name: Setup Go
              uses: actions/setup-go@v2
            - name: Version
              run: go version
