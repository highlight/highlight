name: Firstload
on:
    push:
        paths:
            - 'packages/firstload/**'
            - '.github/workflows/firstload.yml'
jobs:
    firstload:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3
            - uses: pnpm/action-setup@v2
              with:
                  version: 7.11.0
            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: 16.14.2
                  registry-url: https://registry.npmjs.org
                  cache: 'pnpm'
            - name: Install dependencies
              run: pnpm install
            # - name: Run tests
            #   run: pnpm --filter firstload test
            - name: Build Bundle
              run: pnpm --filter firstload build
            - name: Confirm new version
              run: cd packages/firstload && python3 ../../scripts/compare.py -s firstload
            - name: Publish
              if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/beta'
              run: pnpm --filter firstload publish --access public
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
