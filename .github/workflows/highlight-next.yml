name: highlight-run/next
on:
    push:
        paths:
            - 'packages/highlight-next/**'
            - '.github/workflows/highlight-next.yml'
jobs:
    highlight-next:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3
            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: 16.14.0
                  registry-url: https://registry.npmjs.org
                  cache: 'yarn'
            - name: Install dependencies
              run: yarn
            # - name: Run tests
            #   run: yarn workspaces foreach highlight-next test
            - name: Build Bundle
              run: yarn workspaces foreach highlight-next run build
            - name: Confirm new version
              run: cd packages/highlight-next && python3 ../../scripts/compare.py -s highlight-next
            - name: Publish
              if: github.ref == 'refs/heads/master'
              run: yarn workspaces foreach highlight-next publish --access public
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
