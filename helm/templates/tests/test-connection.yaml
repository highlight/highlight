apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "highlight.fullname" . }}-test-connection"
  labels:
    {{- include "highlight.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "highlight.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
