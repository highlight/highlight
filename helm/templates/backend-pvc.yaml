apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "highlight.fullname" . }}-backend-pvc
spec:
  resources:
    requests:
      storage: {{ .Values.backend.config.storageSize }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce