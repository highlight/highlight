apiVersion: v1
kind: Service
metadata:
  name: {{ include "highlight.fullname" . }}
  labels:
    {{- include "highlight.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "highlight.selectorLabels" . | nindent 4 }}
