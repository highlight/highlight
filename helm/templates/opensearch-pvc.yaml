apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "highlight.fullname" . }}-opensearch-pvc
spec:
  resources:
    requests:
      storage: {{ .Values.opensearch.config.storageSize }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce