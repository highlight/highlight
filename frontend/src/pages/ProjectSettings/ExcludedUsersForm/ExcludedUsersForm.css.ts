import { colors } from '@highlight-run/ui/colors'
import { style } from '@vanilla-extract/css'

export const highlightMatch = style({
	backgroundColor: colors.y4,
	borderRadius: 4,
	color: colors.n12,
	padding: '0 2px',
})
