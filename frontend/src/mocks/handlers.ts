// TODO: Add video on how to use this
// import { graphql } from 'msw';
// import GetSessionPayload from './responses/GetSessionPayload.json';

export const handlers = [
    // Handles a "GetUserInfo" query
    // graphql.query('GetSessionPayload', (req, res, ctx) => {
    //     return res(ctx.data(GetSessionPayload));
    // }),
];
